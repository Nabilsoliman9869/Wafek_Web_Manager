openssl_conf = openssl_init

[openssl_init]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
# السماح بتوافق TLS قديم لبعض خوادم SQL القديمة
MinProtocol = TLSv1
CipherString = DEFAULT:@SECLEVEL=1

